'use strict';

import MultiColSlider from '../multi-col-slider';

describe('MultiColSlider View', function() {

  beforeEach(() => {
    this.multiColSlider = new MultiColSlider();
  });

  it('Should run a few assertions', () => {
    expect(this.multiColSlider);
  });

});
