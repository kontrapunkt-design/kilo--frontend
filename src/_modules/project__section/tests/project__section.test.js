'use strict';

import ProjectSection from '../project__section';

describe('ProjectSection View', function() {

  beforeEach(() => {
    this.projectSection = new ProjectSection();
  });

  it('Should run a few assertions', () => {
    expect(this.projectSection);
  });

});
