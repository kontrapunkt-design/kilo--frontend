'use strict';

import ThumbnailProject from '../thumbnail--project';

describe('ThumbnailProject View', function() {

  beforeEach(() => {
    this.thumbnailProject = new ThumbnailProject();
  });

  it('Should run a few assertions', () => {
    expect(this.thumbnailProject);
  });

});
